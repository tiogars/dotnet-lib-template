name: Release

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number to release'
        required: true
        default: '1.0.0'

jobs:
  build:

    runs-on: ubuntu-latest

    permissions:
      contents: write
      packages: write

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8
        cache: true
        cache-dependency-path: MyLibTemplate.Console/MyLibTemplate.Console.csproj
    
    - run: dotnet restore --use-lock-file

    - run: dotnet build --no-restore --configuration Release

    - run: dotnet publish MyLibTemplate.Console/MyLibTemplate.Console.csproj -c Release -r win-x64 --self-contained true
    - run: zip -r MyLibTemplate.Console.zip ./MyLibTemplate.Console/bin/Release/net8.0/win-x64/publish/*

    - run: dotnet publish MyLibTemplate.Web/MyLibTemplate.Web.csproj -c Release -r win-x64 --self-contained true
    - run: zip -r MyLibTemplate.Web.zip ./MyLibTemplate.Web/bin/Release/net8.0/win-x64/publish/*

    - run: gh release create ${{ inputs.version }} MyLibTemplate.Console.zip MyLibTemplate.Web.zip --title "Release ${{ inputs.version }}" --notes "${{ inputs.version }}"
      env:
        GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}